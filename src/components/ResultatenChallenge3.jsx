import React from "react";
import { useHistory } from "react-router-dom";
import { inject, observer } from "mobx-react";

const ResultatenChallenge3 = (regio, answer) => {
    console.log(regio.value);
    return (
        <p>
            resultaat
        </p>
    )
}

export default (ResultatenChallenge3);